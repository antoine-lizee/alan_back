<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :siret %><br>
    <%= f.text_field :siret %>
  </div>
  <div class="field">
    <%= f.label :plan_id %><br>
    <%= f.select(:plan_id) do %>
      <% Plan.all.each do |p| -%>
        <%= content_tag(:option, p.name, value: p.id) %>
      <% end %>
    <% end %>
  </div>
  <div class="field">
    <%= f.label :ratio %><br>
    <%= f.range_field(:ratio, in: 0..1, :step => '.1') %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
